
from setuptools import setup

setup(
    name='gym_sted',
    version='0.0.1',
    install_requires=[
        "gym",
        "scikit-learn",
        "scikit-image",
        "numpy",
        "scipy",
        "matplotlib"
    ]  # And any other dependencies foo needs
)
